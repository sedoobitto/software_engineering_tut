#include <stdio.h>

/**
 * main - Prints a sentence using puts instead of printf
 *
 * Return: returns 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	puts("Programing is like building a multilingual puzzle \n");

	return (0);
}
