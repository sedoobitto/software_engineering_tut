#include "main.h"

/**
 * _isupper - Checks for uppercase character.
 * @c: Character to be checked.
 *
 * Return: 1 on success, otherwise 0.
 */

int _isupper(int c)
{
if (c > 64 && c < 91)
{
	return (1);
}
else
{
	return (0);
}

return (0);
}

